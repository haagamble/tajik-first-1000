import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Deployment config (uncomment for GitHub Pages):
  // output: 'export',
  // basePath: '/tajik-vocab-app',
  // trailingSlash: true,
  // images: {
  //   unoptimized: true
  // }
};

export default nextConfig;
